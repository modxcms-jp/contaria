# ExecPlan運用ガイド

この文書は、このリポジトリで使う ExecPlan の必須要件を定義します。ExecPlan は、AI と人間の両方が同じ設計・実装手順を追える実行可能な設計書です。読者は「現在の作業ツリー」と「その ExecPlan だけ」を持っている前提で書いてください。過去の会話や暗黙知に依存してはいけません。

## いつ使うか

次のいずれかに当てはまる場合は、実装前に ExecPlan を作成します。

- 複数ファイルにまたがる変更
- 新規機能の追加
- 既存仕様へ影響するリファクタリング
- セキュリティ、検証、状態管理に関わる変更
- 実装に 30 分以上かかる見込みがある作業

文言修正、コメント修正、明らかに局所的な小修正は例外です。

## 進め方

1. 先に `ROADMAP.md` を読み、対象タスクの位置づけと前提タスクを確認する
2. `docs/execplans/EXECPLAN_TEMPLATE.md` から新しい ExecPlan を作る
3. 関連コード、要件、既存文書を調査し、雛形の空欄を具体化する
4. 実装前に、目的、対象範囲、非対象、検証方法が読んだだけで分かる状態にする
5. 実装しながら Progress を更新し、判断や逸脱があればその場で追記する
6. 完了後は、結果と残課題を記録し、必要なら `ROADMAP.md` の状態も更新する

## 必須ルール

- ExecPlan 単体で着手できること。別の会話ログ参照を前提にしない
- 要件、コードパス、設定ファイル、影響範囲を実ファイル名で示す
- SOLID、DRY、SSOT、YAGNI、KISS、PIE に反する近道を採らない
- AI が 1 ファイルへ処理を寄せそうな場合は、分割方針を先に書く
- 既知の不確実性、保留判断、追加調査点を隠さない
- テスト、確認手順、未実施項目を明確に分ける

## Progressの書き方

Progress は時系列で追記します。各行は日本時間で記録し、次の形式に統一します。

```text
- 2026-07-12 15:30 JST: 要件と既存コードを確認し、対象範囲を確定
```

完了済み、進行中、保留の区別が読み取れるよう、内容は具体的に書いてください。

## 良いExecPlanの条件

- 背景を知らない人が読んでも目的と完了条件が分かる
- 実装順と検証順が明確で、手戻りしにくい
- 変更対象と非対象が分かれている
- 途中で進捗を見れば、現状と残作業が分かる
- 実装後のレビュー観点まで含まれている

## ファイル配置

- 全体ロードマップ: `ROADMAP.md`
- ExecPlan 雛形: `docs/execplans/EXECPLAN_TEMPLATE.md`
- 個別 ExecPlan: `docs/execplans/` 配下に `NNN-短い名前.md` 形式で追加

番号は `ROADMAP.md` の項目番号に合わせ、例として `001-core-architecture.md` のように命名します。
