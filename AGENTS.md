# Repository Guidelines

## プロジェクト構成

このリポジトリは要件定義先行です。現在の正本は [docs/requirements.md](/home/yamamoto/ghq/github.com/modxcms-jp/contaria/docs/requirements.md) です。まだ `src/` や `tests/` はありません。実装を始める際は、本体コードを `src/`、テストを `tests/`、サンプルや補助資料を `docs/` または `fixtures/` に分けてください。

## ビルド・テスト・開発コマンド

現時点ではビルド、テスト、配布用のスクリプトは未導入です。文書変更時はまず次を使って確認します。

- `git diff -- docs/requirements.md AGENTS.md`: 変更範囲の確認
- `markdownlint docs/requirements.md AGENTS.md`: Markdown の体裁確認
- `rg "TODO|FIXME" docs`: 未整理のメモ確認

`Makefile`、Composer、npm scripts などを追加したら、この節に追記してください。

## 記述スタイルと命名

Markdown は短い節と明確な見出しで簡潔に書きます。例はコードフェンス、パスや属性名はバッククォートで示してください。日本語文書を基本とし、不要な英語混在は避けます。将来 PHP を追加する場合は PSR-12、4 スペースインデント、責務が分かる名前を採用します。

## AI駆動開発の方針

SOLID、DRY、SSOT、YAGNI、KISS、PIE を重視します。AI 生成コードを 1 ファイルへ寄せすぎず、解析、検証、送信、保存、描画などは責務が分かれた時点で分割してください。重複ヘルパー、設定の二重管理、暫定分岐の残存はそのまま取り込まないでください。

AI への依頼は、対象ファイル、制約、責務境界を先に明示してください。コスパ重視で、文書整理や小さな修正は軽量モデル、要件が曖昧な設計判断、横断変更、セキュリティ確認、難しい不具合調査だけ高推論モデルを使う運用を基本にします。

## ExecPlan運用

小さな実装や局所的な文言修正を除き、開発は ExecPlan 方式を基本とします。複雑な機能追加、大きなリファクタリング、複数ファイルにまたがる変更では、実装前に必ず `PLANS.md` を読み、`docs/execplans/EXECPLAN_TEMPLATE.md` をもとに個別の ExecPlan を作成してください。

全体の順序管理は `ROADMAP.md` を正本とし、ExecPlan は原則としてその順番に沿って進めます。各 ExecPlan には進捗欄を設け、タイムスタンプは日本時間で `YYYY-MM-DD HH:MM JST` 形式に統一してください。

## サブエージェントとスキル

このリポジトリでは、まずスキルを使って定型ワークフローを再利用し、サブエージェントは必要時だけ補助的に使います。標準の配置先は `.agents/skills/` と `.agents/subagents/` です。

サブエージェントは、コード調査、テスト観点抽出、セキュリティ観点確認のような独立した read-heavy タスクに限定してください。実装の書き込み責任は原則として親エージェントが持ち、同時に使うサブエージェント数は 2 から 3 までを目安にします。

## テスト方針

自動テストはまだありません。それまでは、新しい仕様や判断を `docs/requirements.md` に反映することを最低条件とします。テスト導入後は `tests/` を本体構成に対応させ、`FormParserTest.php` のように対象が分かる名前を付けてください。

## コミットとプルリクエスト

まだ履歴が薄いため、ここで基準を作ります。コミットメッセージは `docs: 添付ファイル要件を明確化` のように短く具体的に書き、無関係な変更を混ぜないでください。PR では変更内容、関連する要件節や Issue、仕様やフォームマークアップに影響する場合の例を示してください。

## セキュリティと設定

すべての変更は `docs/requirements.md` の方針に合わせます。設定ファイルから任意コードを実行できる形を導入しないこと、内部パスや例外詳細を利用者へ露出しないこと、サーバー側検証を弱めないことを守ってください。
